laptop:
  models: lenovo, hp, dell, apple, asus, microsoft, toshiba, sony, samsung, msi, lg, fujitsu, panasonic, huawei, razer, google, intel, gigabyte, alienware, xiaomi, vaio, nec, medion, sharp, gateway, ibm, compal, cyberpowerpc, eurocom, xotac, vizio, haier, positivo, jumper, chuwi, prestigio, everex, motion momputing, hasee, maxdata, aorus, teclast, malibal, system76, byond, tuxedo computers, nextbook, vinga, emdoor
time:
  offset: 1688237497
update:
  time: 36000000
lotidupdate:
  initialtime: 6000000
lotinformationbyabsent:
    initialtime: 12000000
lotinformationbydate:
    initialtime: 18000000
pdfparser:
  initialtime: 24000000
lotinfo:
  initialtime: 72000000

date:
  url: https://public.api.openprocurement.org/api/2.5/tenders?offset=
lot:
  url: https://public.api.openprocurement.org/api/2.5/tenders/
prozorro:
  url: https://prozorro.gov.ua/tender/
server:
  port:

logging:
  level:
    com.parsing: DEBUG

spring:
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?
    username: postgres
    password: 123
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://${PROD_DB_HOST}:${PROD_DB_PORT}/${PROD_DB_NAME}
#    username: ${PROD_DB_USERNAME}
#    password: ${PROD_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        order_by:
          default_null_ordering: last